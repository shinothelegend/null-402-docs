# null-402-docs

Documentation for **null-402** — private pay-per-call on Stellar.

Planned contents:
- Protocol spec: the x402 flow with a ZK proof in `X-PAYMENT`
- Circuit spec: public/private signals, the payment relation
- Contract API: pool (`deposit` / `is_known_root` / `withdraw`) + verifier (`verify`)
- SDK reference: `null-402/server` and `null-402/client`
- Threat model + security notes
- Deploy guide: testnet → mainnet

> Stub for now. Filled out alongside Phase 2 (real circuits + contracts).
> Source of truth for SDK usage lives in [`../null-402-sdk/README.md`](../null-402-sdk/README.md).
